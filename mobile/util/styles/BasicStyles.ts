import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  centerColumnView: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  centerRowView: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 20
  }
});
